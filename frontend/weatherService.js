/*import axios from 'axios';

// URL de ton API Backend Django
const API_URL = 'http://localhost:8000/api/weather/';  // Remplace par l'URL réelle de ton API

// Crée une fonction pour récupérer les données météo
export const getWeather = async () => {
  try {
    const response = await axios.get(API_URL);  // Envoi une requête GET à ton API
    return response.data;  // Retourne les données météo reçues
  } catch (error) {
    console.error('Erreur lors de la récupération des données météo:', error);
    throw error;  // Lève une erreur en cas d'échec
  }
};*/